# MAS Audit

Guide d'utilisation pour l'audit de systèmes multi-agents (MAS).

## Installation

```bash
pip install -r requirements.txt
```

## Configuration

Copiez `.env.example` vers `.env` et configurez vos API keys.

## Structure

- `mas_runtime/` - Runtime MAS avec adaptateurs
- `seeds/` - Instructions seed pour les tests
- `solvers/` - Inspect solvers
- `scorers/` - Inspect scorers
- `examples/` - Exemples d'utilisation
- `scripts/` - Scripts utilitaires
